class AppConstants {
  AppConstants._();

  // App metadata
  static const String appName = 'FixBuddy';
  static const String appVersion = '1.0.0';

  // Remove any hardcoded Supabase credentials — use .env and SupabaseService instead
}
