
  # User system design

  This is a code bundle for User system design. The original project is available at https://www.figma.com/design/mAGIjafo0mzGyiBMWmDmvQ/User-system-design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  